[
    {mapreduce_wordcount, []},
    {mapreduce, [
        {known_nodes, "${KNOWN_NODES}"},
        {node_directory, "${NODE_DIRECTORY}"},
        {public_directory, "${PUBLIC_DIRECTORY}"},
        {processing_power_multiplier, "${PROCESSING_POWER_MULTIPLIER}"}
    ]},
    {kernel, [
        {logger_level, ${LOGGER_LEVEL}}
    ]}
].
